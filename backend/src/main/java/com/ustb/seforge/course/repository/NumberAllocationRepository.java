package com.ustb.seforge.course.repository;

import com.ustb.seforge.course.domain.NumberAllocation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NumberAllocationRepository extends JpaRepository<NumberAllocation, Long> {}
