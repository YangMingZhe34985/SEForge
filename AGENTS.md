# SEForge · Codex 项目指令引导

<!-- 模板：已有 AGENTS.md 时合并以下条目，不要覆盖原有规范。 -->

开始任何任务前，完整读取仓库根目录 `codex.md`，再读取适用的范围清单与当前交接记录。文件过长时分段读取，不得只读开头。

当前默认任务是路线图 **Phase 1 的只读复核与范围收口**；不是产品优先级 P0/P1 的全部实现。阶段 3 的交接在 `phase-0-1-summary.md` 的 2026-09-24 修订记录中，需定位真实路径并完整阅读。

首次接手先输出差异、证据、最小任务清单和验收计划。未经用户批准具体清单，不修改业务代码、配置、迁移或测试，不继续 Phase 2+。保留用户改动、已有超前代码、安全机制和现有 SEForge 数据。

区分历史 PASS、当前测试结果和用户验收。自动化通过不等于用户已经验收；不得自行跨阶段。

## Code Review Rules

重点检查任务是否越界、认证和资源范围是否由服务端校验、是否保护数据、是否具备当前真实链路证据。不得以 mock 测试或历史报告冒充本轮实机验收；不得通过关闭安全机制、删测试或清库获取成功。
