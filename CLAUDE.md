# SEForge 前端信息架构重构

对当前 SEForge 进行一次前端信息架构重构。

## 核心任务

将当前统一工作台重构为三套明确独立的角色工作台：

- Admin Workspace
- Teacher Workspace
- Student Workspace

要求：

1. 先阅读并理解现有前后端架构、路由、权限模型和已有页面，**优先迁移/复用已有功能，不重复开发**。
2. 保持单一 Vue 前端工程，共享基础组件、API Client、认证体系等；不要拆成三个前端项目。
3. 为三种身份建立独立 Layout、导航、首页和业务入口，登录后根据身份进入对应工作台。
4. 保留现有 `roles / accountType / course role` 权限模型，不为前端重构破坏后端权限设计。
5. 保持现有 UI 设计语言基本一致，并优化明显不合理的交互和页面组织。
6. 审计过程中若发现**明确的 Bug、失效代码、配置错误或影响正常使用的缺陷，可直接修复**；不要进行与本任务无关的大规模重构。
7. 保证现有 Phase 0/1 业务闭环和权限隔离不被破坏。

此外，对以下问题进行修复：
1. AdminBootstrap 非冲突安全：若 admin 用户名或邮箱已被占用，未捕获异常会导致 api 启动崩溃循环（需手动清空 env）。请加入 catch 并降级为告警。
2. **审计日志只写不读**，请修复
3. 课程无更新/归档 UI（后端 PATCH /courses/{id} 存在但前端未调用）

完成后运行必要测试，并简要报告：

**架构调整 / 页面与路由变化 / 顺带修复的问题 / 测试结果 / 仍存在的问题。**