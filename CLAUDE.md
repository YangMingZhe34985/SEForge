# SEForge Phase 0/1 角色端入口与业务闭环修复

## 背景

SEForge 当前学生端已经可以注册、登录并进入主界面，但暂未发现明确的管理员端和教师端入口，导致无法完成：

管理员 → 创建教师 → 教师创建课程/班级 → 生成邀请码 → 学生加入课程

本任务仅处理 Phase 0 / Phase 1 范围，不扩展 Phase 2+ 功能。

## 第一步：先审计，不要立即修改

重点确认：

1. 是否已经存在 ADMIN / TEACHER 对应前端页面、路由或隐藏入口。
2. 是否根据登录用户角色加载不同导航和页面。
3. 后端 Phase 0/1 API 是否已经支持完整业务流程。
4. 默认管理员是否能够正常初始化并登录：

   - Email：`admin@admin.com`
   - Password：`admin12345678`

不要仅根据页面是否可见判断功能不存在，应检查现有源码后再决定修改范围。

## 第二步：补齐缺失功能

如果已有管理员端/教师端，只修复入口、路由、权限或前后端联调问题。

如果确实不存在，则在现有 UI 风格和架构基础上补齐最小可用页面。

### ADMIN

至少支持：

- 管理员登录后进入管理员界面
- 用户/教师管理
- 创建教师账户
- 查看必要的用户与角色信息

### TEACHER

至少支持：

- 教师登录后进入教师界面
- 创建和管理课程
- 创建班级
- 生成/查看课程或班级邀请码
- 查看课程成员
- Phase 1 已有的基础课程管理能力

### STUDENT

保持现有学生端，并确保：

- 使用邀请码加入课程
- 加入后能够正常查看课程

若对应后端 API 已存在，直接复用。
若 Phase 0/1 必需 API 缺失或存在 Bug，可以补齐，但不要开发 Phase 2+ 功能。

## 权限要求

必须保证角色隔离：

- ADMIN 只能访问其授权管理功能
- TEACHER 只能管理自己有权限的课程/班级
- STUDENT 不能访问管理员或教师页面
- 前端路由限制不能代替后端权限校验

禁止通过硬编码角色、关闭 Spring Security 等方式绕过权限系统。

## 验收

最终至少跑通：

`ADMIN 登录 → 创建 TEACHER → TEACHER 登录 → 创建课程/班级 → 生成邀请码 → STUDENT 使用邀请码加入 → STUDENT 查看课程`

同时验证三个角色的页面入口、路由和 API 权限。

优先复用现有代码，进行最小必要修改，不做无关重构。

完成后简要报告：
**现状审计 → 根因/缺失功能 → 修改文件 → 业务闭环验证结果 → Phase 0/1 仍存在的问题。**