# SEForge · Codex 开发交接与阶段收口规范

> 编写日期：2026-09-24。项目负责人：用户。
> 当前默认模式：`AUDIT_ONLY`。当前目标：复核并收口路线图的 **Phase 1**，同时保护 Phase 0 基线。
> **Phase 0 / Phase 1 不等于产品优先级 P0 / P1。禁止据此扩展到整个首发版本。**
> 本文件依据用户叙述及四份文档编写；编写者没有访问项目源码、执行测试或验证提交。历史结果均为文档记录，不是本轮验证结果。

## 0. 先读这页：授权、范围与停止条件

1. 第一次接手先审计，不直接开发。读取本文件、实际路线图、阶段总结最新修订及相关源码，形成差异清单和验收计划。
2. 首轮只允许读取源码、配置、测试与 Git 状态，以及新增交接/审计文档；不得改动业务代码、迁移、运行配置、测试或已有历史报告。测试只可在明确的隔离测试环境执行，不得写入真实教学数据环境。
3. 用户批准具体任务清单后，进入 `IMPLEMENT_APPROVED_SCOPE`。批准“Phase 1 收口”不等于批准本文件所有产品建议。
4. Phase 2～6 已存在的代码默认保留、冻结，不重写、不补全，也不为了“清理超前代码”删除它们。允许读取来分析当前依赖及风险。
5. 若后续阶段的代码直接导致 Phase 1 无法启动、核心业务受阻或存在可利用的越权，先记录影响；只在批准后做最小兼容/安全修复，不开展该阶段的完整开发。
6. 自动化测试通过只能报告 `READY_FOR_USER_ACCEPTANCE`；只有用户明确验收后才可登记 `USER_ACCEPTED`。没有后续阶段的明确授权，不进入 Phase 2。
7. 平台、运行环境的安全约束始终有效。本文件不授权绕过沙箱、审批、认证、安全控制或真实数据保护。

### 0.1 两个不同维度必须分开

- **需求依据**：用户最新明确决定 → 已批准的本轮范围清单 → 已采纳的业务决策记录 → 路线图阶段定义。
- **事实依据**：当前提交和工作树、实际配置、当前运行证据。历史报告只能提供线索，不能替代当前证据。

代码当前“允许某操作”不等于产品上应当允许；用户期望某功能也不等于该功能已经实现。发现冲突应记录来源、差异和建议，不能悄悄改写历史或自创授权。

## 1. 项目、文档与术语定位

### 1.1 项目背景

SmartSE 原由小团队前后端分开开发。用户先由 Claude Code 完成 monorepo 整理和代码风格统一，当时不改变业务逻辑；后续再按 SEForge 目标推进业务重构。

当前文档描述的架构为 Vue 3 / Vite / TypeScript / Pinia 前端、Spring Boot 模块化单体及独立 worker；业务数据、会话和对象存储分别使用 MySQL、Redis、MinIO。AI、向量、评审等后续能力存在提前实现，但未获得完整阶段验收。[D1][D2][D3]

保留单仓库、共享前端组件和认证体系。不得因为拆分角色工作台就新建三套前端工程、三套用户表或三套认证协议。

### 1.2 首轮阅读顺序

按真实仓库定位并记录路径，不要猜测文件存在：

1. 生效的 `AGENTS.md` / `AGENTS.override.md`、本文件及用户当前任务。
2. 根 `README.md`、后端/前端运行说明、根和子目录构建文件、CI。
3. 真正的路线图。已提供报告引用 **`docs/roadmap.md`**；用户口头也使用过 `roadmaps.md`。检查实际文件名、内容和 Git 历史。
4. `phase-0-1-summary.md`：尤其 **第 9 节 2026-09-23 / 2026-09-24 修订记录**，不能只读第 1～8 节。
5. `phase-2-plus-development-status.md`：只用来知道哪些代码已经存在、哪些风险尚未验证，不把它变成本轮任务清单。
6. SmartSE / SEForge 重构说明及其他设计材料（实际存在时）：区分产品优先级和实施阶段。
7. 本轮相关的身份、安全、课程、资源、路由、API client、测试和迁移代码。

如果 `roadmap.md` 与 `roadmaps.md` 同时存在且矛盾，不凭文件名或修改时间擅自选择。列出差异并保留安全的只读模式，直至确定执行依据。

### 1.3 必须保持的区分

| 名称 | 含义 | 当前处理 |
| --- | --- | --- |
| 用户叙述的阶段 1 / 2 / 3 | 三次重构与协作历史 | 交接背景 |
| Roadmap Phase 0 | 工程基线 | 保护并回归 |
| Roadmap Phase 1 | 身份、RBAC、课程与原始资源闭环 | 当前收口对象 |
| 产品优先级 P0 / P1 | 首发需求集合，跨越多个 Phase | 不是本轮全部执行目标 |
| Roadmap Phase 2～6 | AI Core、摄取/RAG、作业、Review、统计/发布 | 已有实现保留，未经授权不推进 |

即使路线图标题包含“P0/P1 可用版”，也必须按其中的 Phase 章节界定本轮工作。

## 2. 必须继承的协作历史与最新状态

### 2.1 执行偏航历史

Codex 曾把“完成 Roadmap Phase 0 / Phase 1”误解为完成重构说明全部 P0 / P1，导致长时间无法收敛；用户中断并重新限定范围。以后不得以“完善平台”“形成完整闭环”为由自主扩大到所有阶段。

### 2.2 阶段 3 已记录的修复

以下仅是 2026-09-24 文档中记载的事实。接手时检查相关修改是否仍存在、是否被覆盖或回退；没有复现失败，不要重复重写。[D2 §9]

| 文档中的变更 | 文档记录的原因或处理 | 报告中的提交线索 |
| --- | --- | --- |
| Docker 中学生注册 403 | 根因是 CORS / 转发 Host 与公共端口，不是应当关闭 CSRF；nginx 使用 `$http_host`，Compose 转发允许 Origin 配置 | `f2e8df2` |
| Vite 开发环境无法连接 API | `.env.example` 没有被 Vite 自动加载，代理未启用；提交 `frontend/.env.development`，默认代理至本机后端 | `b5f7c00` |
| 安全回归 | 非白名单 Origin 拒绝、真实 Cookie CSRF 注册流程、测试上下文污染修复 | `1abc3ff`、`0895ef3` |
| 管理员入口不可见 | 引导变量未配置导致系统没有 ADMIN 账号；文档记录曾在本地配置中解决 | 本地 `.env` 不入库 |
| 三角色工作台 | `/admin`、`/teacher`、`/student`，共享 `WorkspaceShell`、API Client 和认证状态；旧扁平路由重定向 | `dc63828` |
| 管理员引导加固 | 邮箱/用户名冲突、密码不合规时记录告警而非阻塞启动 | `1997d54` |
| 管理员审计日志读取 | 分页审计接口及管理端页面 | `7f8fa89` |
| 课程编辑与归档 UI | 接通既有课程 PATCH 接口；文档记录教师和管理员均有相关入口 | 核实实际实现 |

### 2.3 不得错误使用的验收证据

[D2] 曾判定 Phase 0 / Phase 1 PASS，最新修订记录后端 147 tests、3 skipped，前端 Vitest 21 tests、mock E2E 9 场景，以及另一次实机验收。这些数字只属于报告快照，不能写成本轮运行结果。

用户目前仍认为 Phase 1 尚未完全完成。因此接手结论是：**已有可参考的历史基线，当前待复核、待界定新增要求、待用户重新验收**；既不能直接宣布完成，也不能假定历史工作全部无效。

文档已知未处理项包括账号删除/密码重置、邀请码吊销、教学班更新/删除、课程删除、审计筛选/导出、归档语义，以及测试环境残留数据和 Testcontainers 跳过项。[D2 §9.5]

“没有某个 CRUD 接口”不自动构成原 Phase 1 的缺陷。首先核对原验收要求；例如课程硬删除未必是合理需求。

## 3. 不可破坏的工程与业务基线

- 使用当前仓库的 Java、Node、依赖和锁文件版本。本轮不顺手升级框架、不更换 npm、不重新格式化全仓库。
- API 沿用 `/api/v1`、统一响应与分页、正确 HTTP 状态及 traceId。
- 业务操作者来自服务端认证 Principal。请求内的目标用户 ID 可以作为被管理对象标识，但不能冒充操作者。
- 保留 BCrypt、Redis Session、Cookie 安全属性、CSRF、登录限流和服务端授权。不能用禁用安全机制“修复”联调。
- 全局角色保持 `ADMIN/USER`，课程角色保持 `TEACHER/TA/STUDENT`，除非单独批准权限模型迁移。[D4 公共契约]
- Schema 由 Flyway 管理。已有 SEForge 数据必须保护：不能重写已执行的迁移、清库重建或删除生产卷以获取全绿测试。原计划“不迁移 SmartSE 数据”不等于允许删除现在的 SEForge 数据。
- Phase 1 只要求原始资料上传、登记、授权下载及其必要管理；AI、Embedding、Milvus 不可用时，核心身份/课程业务仍应可用。
- 复用现有 Shell、公共组件和 Application Service；共享实现不代表不同角色拥有相同操作权限。

## 4. 角色、资格与权限：目标业务规则

> 本节除明确引用原契约的内容外，是建议的目标设计。新增行为需进入批准的范围清单，不代表允许一次全部实现。

### 4.1 分开三个概念

**平台权限**决定谁可以管理账号、学期及全局治理；**教学资格**决定新教师尚无课程时能否进入教师工作台并创建课程；**课程成员角色**决定某人可以操作哪一门课程、哪些教学班。

先查清当前 `roles`、`accountType`、课程成员关系及建课权限的真实含义。现有 `accountType` 若是后端维护的教学资格，可以复用；不得仅由浏览器传入，也不得因为“还没有任何课程 TEACHER 成员关系”就让新教师无法建第一门课。

不直接把 `TEACHER` 加成新的全局角色，也不直接把单值 `accountType` 改成多值模型。需要支持多重身份时，先说明迁移、兼容及存量数据影响。

### 4.2 权限基本规则

授权必须结合：账号有效性 + 所需能力 + 目标资源所属课程/教学班 + 有效成员关系 + 资源状态。前端导航、登录入口和本地缓存都不产生授权。

- 教师 A 不能因为有教学资格，就操作教师 B 的课程。
- 本课程教师不自动获得其他课程的教师权限。
- TA 的范围必须明确；默认不拥有账号治理、教师任命、课程负责人转移或最终成绩发布权。
- 学生只能读本人获授权的内容、本人作业与已公开成绩，不读其他学生成绩或其他班级专属内容。
- 管理员能治理所有课程，不默认替所有教师编辑作业、批改或浏览学生私密辅导对话。
- 管理员兼任教师，需要显式授予教学资格及相应课程成员权限；管理员身份本身不是所有教学写操作的通行证。
- 若当前实现与上述管理员边界不同，列为待批准的行为调整，不在“整理菜单”时偷偷改变后端契约。

### 4.3 课程与教学班

建议首发按“学期 → 一次具体开课 → 教学班”组织：一次开课有负责人、课程资料、章节、知识点和多个教学班；成员关系及班级专属内容必须具备明确范围。

这是概念说明，不断言当前 `Course` 已按该语义设计。先核实 `Course / CourseClass / CourseMember`；只有跨学期课程模板与开课实例确实需要分离，才单独讨论 `CourseOffering` 等模型，不在本轮重建全部课程域。

共享课程资料可对课程成员开放；班级公告、班级作业及名单应按目标班级约束。邀请码可绑定课程或班级，但加入语义必须唯一且有测试。

## 5. 登录与工作台入口

### 5.1 建议交互

```text
/login                 学生 / 教师选择；普通页面不提供管理员选项
/login/admin           独立管理员登录入口
/student/**            学生工作台
/teacher/**            教师工作台
/admin/**              管理控制台
```

具体路径是用户提出的目标。复用认证协议、Cookie / Session 和组件，不复制三套认证系统。

“选择教师”表示访问教师门户的意图，不是授予 TEACHER 身份。后端验证账号密码和所需资格；不接受客户端提交的角色列表作为授权来源。建议在入口资格检查成功后才完成该入口登录流程；失败不得生成管理员权限会话。

身份不匹配的信息应在凭据验证后给出，不在认证前泄露账号身份。自动分流仍可用于根路径、已有会话和深链接恢复，但不得覆盖用户明确选择的合法入口。

管理员独立 URL 只是入口隔离，不是安全措施的替代。普通用户直接访问管理 API 必须被服务端拒绝。

### 5.2 必测边界

- 学生选择教师、教师访问管理端、普通用户伪造 `role/accountType/userId` 均不得提权。
- 刷新、深链接、退出后后退、会话过期、角色被撤销后，不出现越权内容或导航死循环。
- 所有重定向只接受经过校验的站内目标，不能产生任意外部跳转。
- 未认证访问受保护 API、已认证但无权限、跨课程资源不可见等，按当前 API 契约分别断言；不要将全部错误机械统一为 403。
- 具有多种合法资格的账号，只显示允许的工作台；切换工作台不增加资格，也不自动建立任意课程成员关系。

## 6. 管理员与教师完整功能蓝图

> **蓝图不是当前执行清单。** 本轮只实现第 7 节经批准的任务。

### 6.1 管理员：身份、教务治理、全局结果

| 模块 | 目标能力 | 必须守住的边界 |
| --- | --- | --- |
| 教师账号 | 单个创建、批量导入、检索、启用/停用、教学资格管理、密码重置、课程交接 | 不允许公开注册教师；停用前处理课程负责人；不暴露密码 |
| 学生账号 | 全局检索、导入/创建、资料校正、启停、重置凭据、查看课程归属 | 不代写学生提交；保留成绩、提交与审计历史 |
| 学期 | 创建、维护、设定当前学期、结束与归档 | 学期结束不自动删除课程或成绩 |
| 全部课程 | 查看、按学期/负责人筛选、调整负责人、处理归档/恢复、异常治理 | 治理权限与日常教学编辑权限分开 |
| 成绩汇总 | 查看教师确认后的成绩、缺报清单、班级/课程/学期汇总、导出、最终锁定 | 不采纳未确认 AI 分数；不静默修改教师分项成绩 |
| 审计 | 账号、角色、课程治理、导入/导出、成绩锁定与更正记录 | 审计不可任意篡改；不记录口令、Session、完整敏感对话 |
| 平台运行 | 业务健康、容量和必要 AI 配额概览 | 不为看统计开放学生原始聊天、草稿和无关个人数据 |

管理员首页应突出账号待处理、课程异常、成绩缺报等真实数据。数据尚未建立时显示空态/未启用，不用随机数或硬编码成功统计。

**导入建议流程**：上传模板 → 校验必填项、唯一标识、格式与重复项 → 预览新增/冲突/失败行 → 确认 → 持久化结果 → 导出错误明细。规定部分成功或整批失败策略，重复请求不能创建重复账号；导入学生文件不能借字段提升为管理员。首发可优先实现受控 CSV，复杂 Excel 和异步大批量另行安排。

**账号生命周期建议**：先实现停用/恢复和安全凭据重置，谨慎提供硬删除。撤销权限或停用账号后，对存量会话应有明确、可测试的失效策略。凭据重置优先采用短时一次性令牌或受控临时凭据并强制修改，不共享固定默认密码。

### 6.2 教师：本课程教学工作闭环

| 模块 | 目标能力 | 必须守住的边界 |
| --- | --- | --- |
| 我的课程 | 建课、维护教学介绍、章节、知识点、安排课程节奏 | 只操作授权课程，遵守学期与归档状态 |
| 教学班 | 建班、维护班级、名单、加入/移除成员、助教授权 | 管理的是课程成员关系，不是全局账号或系统角色 |
| 邀请码 | 生成、分享、有效期、人数限制、吊销/轮换 | 只授予固定的学生加入权限，不接受客户端升级为教师 |
| 课程资料 | 上传、分类、章节关联、可见性、版本替换、下载、撤下 | Phase 1 是原始资源；解析、索引和 RAG 属后续阶段 |
| 课程公告 | 草稿、发布、修改、撤回；可指定课程或班级范围 | 学生不看未发布草稿或其他班级专属公告 |
| 作业 | 题目、Rubric、发布、截止、重交、延期、收取与反馈 | 对已发布规则的变更必须可追溯，保护已提交内容 |
| AI Tutor | 配置允许操作、提示边界和开放完整答案条件 | AI 不绕过教学策略，不替学生直接提交 |
| 批改 | 人工评分，或查看 AI 建议后逐项确认/覆盖并记录原因 | 教师对最终教学评价负责；AI 不自动发布最终分数 |
| 成绩册 | 查询、汇总、发布本课程成绩、处理更正、导出授权班级数据 | 不能读其他课程学生成绩；发布和锁定需分清 |
| 教学分析 | 完成率、缺交、薄弱知识点、成绩分布、课程问答反馈 | 统计口径明确，匿名汇总优先，不能使用假数据 |

同一共享 CourseWorkspace 可以服务不同角色，但不同角色的菜单、可见字段、可执行操作和默认工作流必须不同。优先复用表格、上传器等组件，而不是给管理员呈现完整教师教学台再隐藏少量按钮。

### 6.3 成绩业务的责任链

目标流程：学生提交 → 教师评分（AI 建议可选）→ 教师确认分项 → 按版本化规则汇总总评 → 教师发布 → 管理员进行最终汇总/锁定/导出。

- AI 建议、教师确认分数、课程总评与归档快照应区分，不能互相覆盖。
- 当前文档描述学生可见 `CONFIRMED` 成绩。若增加独立“发布”状态，是新增契约，不能假称已有，更不能在 Phase 1 顺手迁移。
- 总评可采用 `100 × Σ(权重 × 分项得分 / 分项满分)`，权重和为 1；是否使用此公式由课程计分规则决定。
- 缺交、未批改、缓交、免修、退课与有效 0 分是不同状态；不得把所有空值都按 0 分统计。
- 多次提交取哪次、迟交扣分、四舍五入和权重变更必须有明确规则及快照版本。
- 总评锁定后更正，应记录原值、新值、原因、提出者、批准者和时间；管理员不能无痕改写成绩。
- 归档课程与锁定最终成绩不是同一个状态机；不能用一个 `ARCHIVED` 同时代替所有业务状态。

这条链涉及 Phase 4、Phase 5、Phase 6 及新增教务需求，不是本轮 Phase 1 的隐含任务。

### 6.4 归档、成员移除与删除

文档中现有归档行为仅阻止新成员加入，其余语义待定义。[D2 §9.5]

建议目标：归档课程对原成员保留授权只读访问，不再加入新成员，也不允许新增/修改教学内容或提交；需要继续批改、更正成绩时走明确恢复或专项更正流程。若业务需要“停止招生但继续授课”，那是与归档不同的状态。

这会影响后续作业、Review 和后台任务；因此先列出所有写入口及处理中任务策略，再决定是否纳入单独修订包。不得只在 UI 禁用按钮后宣称完成只读归档。

优先停用账号、退出课程、撤下资源和归档课程。确需硬删除时，必须定义关联数据、对象文件、保留策略与审计，不能级联删除历史成绩来让操作成功。

## 7. 本轮范围：基线、修订、延期三张清单

### 7.1 原 Phase 1 基线候选任务

先审计下面条目；已满足的只回归，不重复开发。

| ID | 交付闭环 | 本轮验收方向 |
| --- | --- | --- |
| PH1-B01 | 认证与开发/部署联调 | Vite 代理、Docker 同源注册、真实 Cookie/CSRF、登录/注销/会话读取 |
| PH1-B02 | 管理员最小后台 | 管理员引导、创建教师、学期与课程审计，管理 API 真实授权 |
| PH1-B03 | 教师课程与班级 | 新教师可创建首门课程，创建教学班、邀请码、维护本课程基本内容 |
| PH1-B04 | 课程原始资料与公告 | MinIO 上传/登记/授权下载，必要管理与审计；无 AI 依赖 |
| PH1-B05 | 学生学习入口 | 学生注册、邀请码加入、正确查看授权课程及资源 |
| PH1-B06 | 服务端权限与回归 | 跨用户、跨课程、跨角色测试，Phase 0 工程/迁移基线不退化 |

### 7.2 用户提出、建议明确纳入的入口与职责修订

| ID | 建议修订 | 范围限制 |
| --- | --- | --- |
| PH1-UX01 | 普通登录显式选择学生/教师，独立 `/login/admin` | 复用认证体系，资格后端校验，不引入新的全局角色模型 |
| PH1-UX02 | 三角色工作台职责去重与入口整理 | 优先修正菜单、页面操作和调用授权，不新建三套前端、不重做全站视觉 |

这些条目来自用户的新要求，不伪称原路线图一直有此验收条件。经审计确认后，作为明确的本轮修订包冻结。

### 7.3 建议单列的 Phase 1 补充项

账号停用/恢复、密码重置、教师/学生批量导入、邀请码吊销、教学班维护、课程负责人交接、归档只读语义、审计筛选/导出：按风险和用户需要逐项决定，可命名为“Phase 1 补充包”或“Phase 1.1”，但不要修改原 Phase 编号。

这些并非默认全部批准。若其中某项是用户本次验收必需，给出独立任务 ID、数据影响和测试，再加入范围锁。

### 7.4 明确延期

未经单独授权，本轮不推进：Embedding 统一路由、Redis/worker 全故障恢复、文档解析/索引/RAG、Tutor、作业系统完整闭环、AI/Sonar Review、最终成绩发布与汇总、Dashboard 聚合、完整生产性能/恢复验收。

更不扩展到 Teacher Agent、在线 Judge、知识图谱、爬虫、学习画像、GitHub/Sprint 等原非首发能力。[D4]

Phase 2+ 已有代码的常规构建回归仍可执行；失败先分类为新增回归、既有失败或环境阻断，不借机开展整阶段修复。

## 8. 审计与开发执行协议

### 8.1 首轮审计输出

记录当前分支、提交 SHA、工作树是否干净、运行方式、关键环境版本，以及实际采用的文档路径。不得打印 `.env`、密码、Cookie、Token 或密钥；日志和配置展示必须脱敏。

建立差异表：

```text
任务 ID | 原始要求/新增要求 | 文档记录 | 当前代码位置 | 当前运行证据
       | 状态 | 缺口/原因 | 最小修复建议 | 是否跨阶段 | 是否已批准
```

状态只用 `PASS_WITH_CURRENT_EVIDENCE / FAIL / PARTIAL / NOT_RUN / BLOCKED_ENV / OUT_OF_SCOPE`。历史 PASS 单独列，不混入本轮 PASS。

首轮交付建议为 `docs/phase-1-reacceptance-audit.md` 和范围清单草案。首次审计后停止业务改动，等待用户批准任务集合；不得把生成计划视为获得实施批准。

### 8.2 范围锁

批准后建立 `docs/phase-1-scope-lock.md`（或已有等价文件），至少写入：

- 实际路线图路径、基线 SHA、批准时间与用户批准内容；没有证据不能自填“已批准”。
- 原始基线任务、新增修订任务、明确排除项及业务决策。
- 每项允许改动的模块、验收标准、环境要求和回滚方法。
- 本轮完成定义与用户验收状态。

拆成若干可独立验收的纵向任务，不按“先把全部后端重写、再把全部前端重写”推进。

### 8.3 每个实施任务的循环

复现或建立失败用例 → 定位最小原因 → 确认属于批准范围 → 修改必要代码 → 定向测试 → 相关集成/浏览器验证 → 检查 diff → 更新证据和交接记录。

连续两次修改仍未消除同一失败时，停止扩大补丁。保留失败输出、列出已排除假设，用新的审查上下文重新定位，必要时建议用户切换模型。

普通实现细节在已批准范围内自行解决；涉及数据删除、权限扩大、状态机/计分规则改变、第三方费用或跨阶段开发时，记录待决事项，只阻塞相关任务，继续其他不受影响的已批准任务。

### 8.4 Git 与协作

- 先检查未提交改动，不覆盖用户或其他代理的修改。不得自动 `reset --hard`、`clean -fd`、强推或清空工作树。
- 不并行修改同一个共享认证、路由或迁移文件。并行仅用于清晰分工的只读审计或独立测试；一个任务由一个主实现者集成。
- 每个逻辑任务形成可审阅的小 diff 和可回退 checkpoint。是否自动 commit 遵从用户已有授权；未获授权则提供提交建议。
- 新增迁移使用新的 Flyway 版本，测试空库和已有 Schema 升级；不假定最新版本永远是 V3。
- 不因整理中文文案、换行符或风格扫描造成全仓库噪声。

## 9. 测试与验收矩阵

### 9.1 测试分层

1. 静态检查/类型/构建和单元测试：确认局部规则及编译正确。
2. HTTP 集成：真实认证 Principal、Cookie/CSRF 和持久化授权，不信任伪造身份头。
3. 真实基础设施集成：本轮关键 MySQL、Redis Session、MinIO 行为。
4. 非 mock 业务 E2E：浏览器连接真实 API 与数据库，覆盖角色链路；供应商 AI 可禁用，因为不是 Phase 1 前提。
5. 用户手工验收：交付清晰步骤、测试账号获取方式和预期结果，由用户判断功能与交互。

mock E2E 只能证明受控 UI 场景，不能替代实际反向代理、Cookie、后端授权及存储链路。API curl 成功也不能替代浏览器的 CORS/Cookie 行为验证。

### 9.2 最低验收场景

以下场景的新增部分只在对应修订获批后生效：

| ID | 场景 | 预期 |
| --- | --- | --- |
| T01 | 全新测试环境，无 AI Key / 无 Milvus | 核心应用启动，账号/课程/资源流程可用 |
| T02 | 管理员引导首次启动及重复启动 | 创建受控管理员，不重复创建；冲突不暴露凭据或造成启动循环 |
| T03 | Vite 本地开发模式 | `/api/v1` 实际到达 API；注册/登录/当前用户/注销可用 |
| T04 | Docker nginx 同源模式及非默认公共端口 | 浏览器真实注册登录可用；Host/Origin/转发端口行为正确 |
| T05 | CSRF 与 CORS 正反例 | 合法 Origin + 有效 CSRF 成功；非法 Origin、缺失/错误 CSRF 按策略拒绝 |
| T06 | 管理员 → 新教师 → 首门课程 → 教学班 → 邀请码 → 资料 | 所有对象落库，刷新后仍存在，资源可读，审计正确 |
| T07 | 新学生注册 → 加入 → 查看公告/资料 | 只出现本人授权内容；重复加入幂等或明确冲突，不重复成员 |
| T08 | 两位教师、两门课程、两个班、两位学生与一个非成员 | 对调任意 courseId/classId/resourceId 无法越权；角色名相同也不能跨范围 |
| T09 | 伪造身份头、请求字段、localStorage、路由参数 | 不能获得管理员或他人课程权限 |
| T10 | 学生/教师选择入口及独立管理员入口 | 成功进入合法工作台；身份不匹配不提权；管理端深链接正确处理 |
| T11 | 注销、Session 过期、刷新与后退 | 会话和敏感缓存正确处理，无无限跳转或旧权限残留 |
| T12 | 授权用户的 MinIO 上传、下载与越权下载 | 文件内容一致；对象不能通过公开地址绕过应用授权 |
| T13 | 邀请码过期/人数上限/吊销与并发加入 | 已实现或批准的限制由服务端执行；单次请求不能改变授予角色 |
| T14 | 账号停用、课程归档、成员退出/角色撤销 | 仅对已批准语义验收；现有会话及后续请求一致执行策略 |
| T15 | 新库迁移和已有 SEForge 测试库升级 | Flyway 正确，已有账号、课程、资源及后续阶段数据不受破坏 |
| T16 | 改动后的前后端基础门禁、相关已存在功能回归 | 不新增失败；既有失败与环境跳过独立披露 |

所有越权测试需要一个可访问相应对象的正例，避免“所有请求都失败”也被误判为授权正确。

### 9.3 环境与命令

从仓库 README、`package.json`、Maven wrapper 和 CI 读取真实命令。本地基础命令线索如下，先确认存在再执行：

```bash
cd backend
./mvnw clean verify

cd ../frontend
npm ci
npm run lint
# typecheck / 单元测试 / E2E 的脚本名以 package.json 和 CI 为准
npm run build
```

Windows 采用实际 wrapper 与 shell；不把 shell 不兼容归类为业务失败。

Compose 必须先确认 `.env` 和目标项目为隔离测试环境，再执行配置校验及启动。不能向输出复制展开后的真实密钥配置，不能使用 `down --volumes` 清理现有数据。

后端、前端、Playwright、Testcontainers 的 skipped / disabled / mock 状态必须逐项标注。缺少 Docker、Redis 或 MinIO 的真实验证时，结论是 `BLOCKED_ENV` 或 `NOT_RUN`，不是“实机已通过”。

原报告曾获准跳过个别本机 Docker 测试，这个历史例外不能自动成为本轮豁免。可提交稳定 Linux CI 的当前执行证据；若仍缺关键证明，只能给出带缺口的候选交付，由用户决定是否接受风险。

### 9.4 证据最小格式

```text
任务/场景 ID：
提交 SHA + 工作树说明：
时间、环境和启动方式：
命令/操作步骤：
真实依赖 / mock 边界：
预期结果与实际结果：
通过/失败/跳过数量与退出码：
脱敏日志、测试报告、截图或 trace 路径：
未验证内容与复验方式：
```

失败用例必须保留原因，不能删测试、放宽断言、批量 skip 或只展示最后一次局部门禁来凑全绿。

### 9.5 测试数据

为测试创建独立数据库/对象前缀和可识别的测试运行 ID，记录本次创建对象。清理只针对明确属于该次隔离测试的数据。

历史报告提到 `probe-*`、`*227151`、`ui-*` 等残留；这些名字不是删除授权。不得仅按模糊前缀自动删除真实环境账号、学期或课程。

## 10. 完成定义与跨阶段放行

状态流建议：

```text
AUDIT_ONLY
  → SCOPE_APPROVED
  → IMPLEMENTING
  → ENGINEERING_VERIFIED
  → READY_FOR_USER_ACCEPTANCE
  → USER_ACCEPTED
```

工程验证至少要求：批准清单已交付；必要的正反例与真实链路通过；Phase 0 无新增回归；缺失环境证据已单独披露；无未处理的阻断缺陷；可部署/回退；文档与行为一致。

只要本轮必需的真实身份、资源或关键权限链路没有验证，就不能标记无保留的 `ENGINEERING_VERIFIED`。非关键例外由用户明确接受并留下记录。

用户验收表单至少包含：用户确认时间、验收提交 SHA、接受的范围、明确接受的已知限制。代理不得代签，不把“没有回复”解释为默认接受。

后续阶段独立开单、提交和验收。可以记录下一阶段风险、整理只读交接，但不自动编码、不变更当前清单。

## 11. 模型、思考强度与额度使用建议

> 本节是项目负责人的配置参考；代理不能声称已自行切换模型，也不能把模型名称当作完成证据。

- 默认实现：GPT-5.6 Sol + Medium，适合边界清晰的 UI、接口接线、局部修复和测试。
- 跨模块认证/权限、状态边界、数据库迁移：先用 GPT-5.6 Sol + High；复杂设计或反复失败时建议用户升级到 GPT-6 Astra。
- Astra 建议先从 Light / Medium 进行范围审计、设计审查或疑难定位；只有确实需要时提高到客户端支持的 High / Extra High。不要默认所有任务最高档。
- Max 用于极少数深度问题；Ultra 会涉及并行代理，应只在任务可清晰分离、没有共享写冲突且用户愿意承担消耗时使用。
- 官方截至 2026-09-24 已加入 GPT-6 Sol；用户可用同类型独立任务比较其与 5.6 Sol 的验收通过率、返工和实际额度，再决定是否换主力，不在本轮强制切换。
- 同一问题连续两次修复未收敛时，优先整理复现、日志、diff 与未决假设，再更换审查上下文或模型，而不是把整个项目交给最高档重写。
- 每次会话只携带当前任务相关代码、范围锁及必要历史；不反复全仓库扫描、不把完整日志和无关 Phase 文档长期堆入上下文。
- 以客户端实际可选模型/强度及用量面板为准。订阅额度不等同于 API 单价，也不保证固定任务数。

以上分工是建议，不是 SEForge 的模型效果实测。[W1][W2][W3]

## 12. 持续交接与最终报告

长期稳定规范保留在本文件；本轮范围和进度放在独立文档，不不断膨胀本文件。

建议维护：

```text
docs/phase-1-scope-lock.md               已批准范围与排除项
docs/phase-1-reacceptance-audit.md       新一轮差异与证据
docs/phase-1-acceptance.md               验收步骤、结果、用户确认
docs/current-handoff.md                当前分支/SHA、已完成、阻塞、下一项
```

文件名可复用现有等价文档，避免再造同义报告。历史 Phase 0/1 总结不被覆盖；采用新增验收记录或获准后追加明确日期的修订。

每次任务结束报告：完成哪些批准任务、改了哪些文件和原因、实际运行哪些测试、哪些未跑或受阻、已知风险、复验步骤、是否需要用户验收。只报告已发生的工作。

不得输出无证据的“全部修复”“完全通过”“生产可用”。不得自动完成下一阶段，也不得承诺会在没有任务机制的情况下后台持续开发。

## 13. 第一次接手应给出的答复内容

先给出以下事实，不直接开始大规模编辑：

1. 实际基线 SHA、工作树状态、读取的文档路径及路线图 Phase 定义。
2. 已确认保留的阶段 3 修改，与需要复验的行为。
3. 原 Phase 1 缺陷、新增入口/职责需求、补充包、Phase 2+ 延期项的分类。
4. 现有代码/测试能证明什么，哪些只有历史报告或 mock 证据。
5. 建议批准的最小任务清单、验收方式及跨阶段风险。

本轮停止在审计交付；用户批准后按第 8 节执行。

---

## 附录：来源与适用性

### 项目来源

- **[D1]** 用户提供的《SEForge》README：架构、开发启动、三工作台、管理员引导、质量门禁。
- **[D2]** 《SEForge Phase 0 / Phase 1 阶段总结报告》：审计日期 2026-09-22，修订日期 2026-09-24；尤其第 9 节修订和已知限制。历史测试结果仅作为历史记录。
- **[D3]** 《SEForge Phase 2+ 开发现状报告》：审计日期 2026-09-22；Phase 2～6 均为 PARTIAL，不能替代后续最新源码审计。
- **[D4]** 《SEForge P0/P1 可用版升级实施计划》：Phase 0～6、公共权限契约及“每个 Phase 单独提交、部署、验收”的约定。实际仓库路径需定位，不能仅凭标题识别执行范围。
- **[U]** 用户 2026-09-24 的说明：三段协作历史、当前未接受 Phase 1 完全完成、希望普通登录区分学生/教师并独立管理员 URL、管理与教学职责分离、偏好 5.6 Sol 日常开发和 Astra 疑难任务。

本文件第 4～7 节的新增流程、状态语义和分工是基于 [U] 的设计建议，不声明四份文档已包含这些全部要求；只有进入用户批准范围才是当轮实施要求。

### 官方产品参考（核实于 2026-09-24）

- **[W1]** Codex 模型及思考强度：`https://learn.chatgpt.com/docs/models`
- **[W2]** Codex 订阅与用量：`https://help.openai.com/en/articles/11369540-using-codex-with-your-chatgpt-plan`
- **[W3]** 模型额度页面：`https://chatgpt.com/codex/pricing/`
- **[W4]** 项目指令发现机制：`https://learn.chatgpt.com/docs/agent-configuration/agents-md`
- **[W5]** GPT-6 Sol / Luna 发布说明：`https://openai.com/index/introducing-gpt-6-sol-and-luna/`

Codex 官方自动发现的是 `AGENTS.md` / `AGENTS.override.md` 及显式配置的 fallback 名称；不能假定仅放置 `codex.md` 就会自动加载。建议把短引导合并到根 `AGENTS.md`，要求每次任务先完整读取本文件。不要覆盖仓库已有规则，并检查子目录 override 是否存在冲突。[W4]
